// Placeholder — PDF loading and rendering service

const PdfService = {
  load: async (uri) => {
    // TODO: load PDF from uri or bundled asset
  },
  getPageCount: async () => {
    // TODO: return total page count
    return 0;
  },
};

export default PdfService;
